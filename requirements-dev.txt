# core
numpy>=2.0
opencv-python>=4.8
scipy>=1.11
pydantic>=2
trimesh>=4
rich>=13
typer>=0.12

# torch – install via the PyTorch selector first; keep here only if you want pip to manage it
# torch>=2.4

# openmmlab (install mmcv/mmengine via OpenMIM; mmpose via pip)
mmengine>=0.10
mmcv>=2.1
mmpose>=1.3

# detection (fork)
pixeltable-yolox>=0.2  # pin to the exact release you test

# tracking
supervision>=0.21      # or trackers>=2.0

# dev
pytest>=7
pytest-cov
ruff>=0.4
mypy>=1.9
